info:
  name: AI Flights Search
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:5050/api/flights/ai-search
  body:
    type: json
    data: |-
      {
        "prompt": "Find me a flight from Copenhagen to London on 2026-07-15"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
